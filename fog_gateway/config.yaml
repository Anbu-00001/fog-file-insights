# Fog gateway configuration (simple, safe defaults)
server:
  host: "0.0.0.0"
  port: 5000

policy:
  null_pct_threshold: 0.30
  min_rows: 1

forward:
  url: "http://localhost:8000/api/ingest/"
  timeout_seconds: 10
  max_retries: 3
  retry_backoff_seconds: 2

auth:
  # Shared token the fog will send to Django
  fog_token: "super-secret-demo-token"

retry:
  # how often the background retry worker wakes (seconds)
  retry_interval_seconds: 20
  # maximum times the local pending file will be retried by the worker
  max_pending_attempts: 10

storage:
  forwarded_dir: "forwarded_files"
  quarantine_dir: "quarantined_files"
  pending_dir: "pending_files"
  max_upload_size_bytes: 5242880
